import 'package:flutter/material.dart';

class Karthik extends StatelessWidget {
  const Karthik({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('karthik'),
      ),
    );
  }
}
